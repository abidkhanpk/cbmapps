// Custom JavaScript for FMECA App

$(document).ready(function() {
    console.log("FMECA App JavaScript loaded.");

    // Example: Add a simple click handler
    $('.nav-link').on('click', function() {
        // console.log('Nav link clicked:', $(this).text());
    });
});
